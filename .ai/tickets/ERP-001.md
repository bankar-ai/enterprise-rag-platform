# ERP-001 — Repository Operating System Foundation

Status: Done
Depends On: None

## Description

Establish the initial `.ai/` skeleton (`tickets/`, `adr/`, `sessions/`, `memory/`, `templates/`, each with a README stub) and rewrite `CLAUDE.md` so it acts as a short operational guide pointing into `docs/` and `.ai/`, instead of holding the full project brief inline. Also introduced `docs/architecture.md`, `docs/engineering-guidelines.md`, and `docs/roadmap.md` to hold the durable reference documentation that used to live entirely in `CLAUDE.md`.

This ticket is backfilled retroactively — the framework it describes didn't exist yet when the work was done (commit `a2f37d6`), so it's being recorded now as ticket #1 for a complete history.

## Acceptance Criteria

- [x] `.ai/README.md` describes the operating system and its subdirectories
- [x] `.ai/{tickets,adr,sessions,memory,templates}/README.md` stubs created, each naming the ticket that will build it out
- [x] `docs/architecture.md`, `docs/engineering-guidelines.md`, `docs/roadmap.md` created
- [x] `CLAUDE.md` shortened to an operational guide referencing `docs/` and `.ai/`

## Notes

Completed in commit `a2f37d6` ("docs: establish repository operating system foundation").
